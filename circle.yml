machine:
  node:
    version: 6.9.0
dependencies:
    post:
        - sudo npm install
        - npm rebuild node-sass
        - npm run build:prod
test:
    override:
        - tar -czf export.tar.gz dist
        - sudo cp export.tar.gz $CIRCLE_ARTIFACTS
